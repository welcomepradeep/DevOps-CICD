---
- name: Configure Jenkins Server
  hosts: jenkins
  become: yes

  roles:

    - common
    - git
    - java
    - docker
    - jenkins

##########################################################

- name: Configure Web Server
  hosts: web
  become: yes

  roles:

    - common
    - docker
    - nginx
